# T2 SDK

This package contains the official Go SDK for building T2 integrations, MCP clients, and agent runtimes.

## Usage

```go
import "github.com/t2-travel-terminal/t2-travel-terminal/pkg/sdk"
```

The SDK is under active development. See `docs/` for the full API specification.
